<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">


    <insert id="insertBatch" parameterType="list" >
        insert into setmeal_dish
        (setmeal_id, dish_id, name, price, copies)
        VALUES
        <foreach collection="setmealDishes" item="sd" separator=",">
            (#{sd.setmealId},#{sd.dishId},#{sd.name},#{sd.price},#{sd.copies})
        </foreach>
    </insert>
    <!-- 正确的 deleteBySetmealId：只根据套餐ID删除关联记录 -->
    <delete id="deleteBySetmealId" parameterType="java.lang.Long">
        delete from setmeal_dish
        where setmeal_id = #{setmealId}  <!-- 仅保留套餐ID作为条件，无其他多余逻辑 -->
    </delete>


    <select id="getSetmealIdByDishIds" resultType="java.lang.Long">
        select setmeal_id from setmeal_dish where dish_id in
        <foreach collection="dishIds" item="dishId" separator="," open="(" close=")">
            #{dishId}
        </foreach>
    </select>

    <select id="getSetmealIdsByDishIds" parameterType="list" resultType="java.lang.Long">
        SELECT DISTINCT setmeal_id FROM setmeal_dish WHERE dish_id IN
        <foreach collection="dishIds" item="dishId" open="(" separator="," close=")">
            #{dishId}
        </foreach>
    </select>




</mapper>
